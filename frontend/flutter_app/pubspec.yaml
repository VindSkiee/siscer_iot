name: plant_iot_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Untuk HTTP request (kirim perintah ON/OFF)
  http: ^1.1.0
  
  # Untuk WebSocket (terima data real-time)
  web_socket_channel: ^3.0.3
  
  # Untuk format tanggal
  intl: ^0.20.2 

  fl_chart: ^1.1.1
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true